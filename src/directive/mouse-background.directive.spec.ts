import { MouseBackgroundDirective } from './mouse-background.directive';

describe('MouseBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new MouseBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
